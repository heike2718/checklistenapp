// tslint:disable-next-line:max-line-length
import { ChecklisteDaten, EINKAUFSLISTE, MODUS_SCHROEDINGER, PACKLISTE, TODOS, ChecklistenItem, MODUS_EXEC, MODUS_CONFIG } from '../model/checkliste';
import { Observable, of } from 'rxjs';
import * as _ from 'lodash';

export function loadCheckliste(id: number, modus: string): Observable<ChecklisteDaten> {
	return null;
}

