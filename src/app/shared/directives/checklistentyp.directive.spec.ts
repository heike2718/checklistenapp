import { ChecklistentypBackgroundDirective } from './checklistentyp.directive';

describe('ChecklistentypDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecklistentypBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
