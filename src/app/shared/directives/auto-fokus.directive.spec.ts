import { AutoFokusDirective } from './auto-fokus.directive';

describe('AutoFokusDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFokusDirective();
    expect(directive).toBeTruthy();
  });
});
